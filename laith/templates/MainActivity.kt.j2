package {{ namespace }}

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.compose.material3.Text
import laith.runtime.PythonRuntime

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        
        PythonRuntime.initialize()
        
        // Schedule periodic tasks (WorkManager)
        // Note: In a real project, this would be imported from the generated Kotlin file
        // We'll assume it's in the same package or globally accessible for now
        scheduleLaithTasks(this)
        
        setContent {
            // This will call the 'main_ui' function generated from Python
            // For now, a placeholder
            Text("Laith App Running")
        }
    }
}
